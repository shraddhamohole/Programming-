//c program to print no is prime or not
#include<stdio.h>
void main()
{
	int n,i,count=0;
	printf("enter any no");
	scanf_s("%d",&n);
	for(i=1;i<=n;i++)
	{
		
		if(n%i==0)
		{
			count++;
		}
	}
		
		if(count==2)
			printf("\n%d is prime",n);
		
		else
			printf("\n%d is not prime",n);
	
	
}
